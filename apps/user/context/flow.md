Auth Flow:

- The user downloads our app
- if already logged in will go to home page else login page
- if he already associated with the same ip address/locally cached, it will show to continue with same account else fresh login
- user can continue with phone number screen following with otp screen
- user can also continue with email
- the user model will be cached locally in the app for future usage
